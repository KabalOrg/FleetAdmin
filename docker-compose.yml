version: '3.8'

services:
  fleet-app:
    build: .
    ports:
      - "8080:8080"
    volumes:
      - ./fleet.db:/root/fleet.db
      - ./credentials.json:/root/credentials.json:ro
    environment:
      - TELEGRAM_BOT_TOKEN=${TELEGRAM_BOT_TOKEN}
    restart: unless-stopped